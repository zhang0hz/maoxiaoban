Create a single horizontal sprite strip for Miu, action `peek`.

Output exactly 6 separate animation frames arranged left-to-right in one row.

Action:

- Miu peeks in from a screen edge.
- Only part of the body appears at first, then head and front paws become visible.
- Use pose and cropping-like placement inside each slot, but do not clip the sprite at the image boundary.
- Gentle curious expression.
- No wall, window, UI, or edge line should be drawn.

Follow `shared-contract.md`.
