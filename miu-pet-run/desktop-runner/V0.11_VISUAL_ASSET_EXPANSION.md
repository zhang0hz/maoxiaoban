# V0.11 Visual Asset Expansion

Implemented:

- Runtime loads new action ids:
  - `blink`
  - `paw-wave`
  - `tail-sway`
  - `yawn`
  - `ear-twitch`
- Behavior now prefers these actions when available:
  - mouse enter: `paw-wave`, fallback `purr`
  - mouse leave: `blink`, fallback `purr`
  - active work / idle: `blink`, fallback `purr`
  - active leisure: `tail-sway`, fallback `purr`
  - long idle: `yawn`, fallback `stretch`
- Added action folders under `phase1-actions/frames`.
- Current V0.11 folders use color-audited source frames so the running app gets stable new action hooks without color drift.
- Added generation contract for future newly generated replacement sprites:

```text
miu-pet-run/phase1-actions/prompts/v0.11-generation-contract.md
```

QA:

- New action folders are included in `color_audit.py`.
- Package install should audit `/Applications/Miu.app/Contents/Resources/frames`.

Note:

- This pass prioritizes stable runtime integration and identity safety.
- Future V0.11.x can replace each derived folder with newly generated rows, one action at a time, with color QA gate before install.
