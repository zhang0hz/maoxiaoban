# V0.11.1 True Redraw

Implemented:

- Added redraw job manifest:

```text
miu-pet-run/phase1-actions/v0.11.1-redraw-jobs.json
```

- Jobs prepared for:
  - `blink`
  - `paw-wave`
  - `tail-sway`
  - `yawn`
  - `ear-twitch`
- Existing runtime already loads and uses these action ids.
- Current installed frames remain identity-safe derived frames until each generated replacement passes review.

Gate:

- Replace one action at a time.
- Compare generated row against `loaf`, `purr`, and `groom`.
- Run:

```bash
python3 miu-pet-run/desktop-runner/color_audit.py \
  --frames-root miu-pet-run/phase1-actions/frames
```

- Package and audit installed app frames before launch.

Note:

- This pass adds the true redraw pipeline and job specs.
- It does not silently install unreviewed generated sprites.
